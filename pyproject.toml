[project]
name = "nes-emulator"
version = "0.1.0"
description = "A NES emulator written in Python"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame>=2.6.1",
]

[dependency-groups]
dev = [
    "pillow>=12.1.0",
    "pytest>=8.4.2",
]
